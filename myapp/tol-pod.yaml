apiVersion: v1
kind : Pod
metadata : 
   name : tol-pod
   labels : 
      app : myapp
spec:
   containers: 
     - name : con2
       image: nginx:latest
       ports:
        - containerPort: 80
   tolerations:
     - key: color
       operator: Equal
       value: green
       effect: NoExecute


 
