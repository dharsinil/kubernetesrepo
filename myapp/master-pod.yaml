apiVersion: v1
kind : Pod
metadata : 
   name : master-pod
   labels : 
      app : myapp
spec:
   containers: 
     - name : con2
       image: nginx:latest
       ports:
        - containerPort: 80
   tolerations:
     - key: node-role.kubernetes.io/control-plane
       effect: NoSchedule
 
